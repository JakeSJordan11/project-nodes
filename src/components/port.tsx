import styles from '@/styles/port.module.css'

export default function Port() {
  return (
    <button
      className={styles.port}
      // onPointerDown={(event) => (
      // event.stopPropagation(), onPortPointerDown(event)
      // )}
      // onPointerUp={onPortPointerUp}
    />
  )
}
