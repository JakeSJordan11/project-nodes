export enum CanvasActionTypes {
  CreateNode = "CREATE_NODE",
  CreateStream = "CREATE_STREAM",
  SelectNode = "SELECT_NODE",
  DragSelection = "DRAG_SELECTION",
  DropSelection = "DROP_SELECTION",
  UpdateNode = "UPDATE_NODE",
  AttemptLink = "ATTEMPT_LINK",
}
